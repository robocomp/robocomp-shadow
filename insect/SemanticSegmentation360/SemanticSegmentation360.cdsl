import "Camera360RGB.idsl";
import "SemanticSegmentation.idsl";
import "VisualElements.idsl";

Component SemanticSegmentation360
{
    Communications
    {
        implements SemanticSegmentation;
        requires Camera360RGB, VisualElements;
    };
    language python;
};

