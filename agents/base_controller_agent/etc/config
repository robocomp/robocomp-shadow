# This property is used by the clients to connect to IceStorm.
Proxies.TopicManager = "IceStorm/TopicManager:default -p 9999"

# Endpoints for subscriptions interfaces
Endpoints.FullPoseEstimationPubTopic = "tcp -p 9878"

# Proxies for required interfaces
Proxies.Lidar3D = "lidar3d:tcp -h localhost -p 11990"
Proxies.GridPlanner = "gridplanner:tcp -h localhost -p 16804"

Agent.id = 666
Agent.name = "base_controller_agent"
Agent.configFile = "shadow_scene.json"

ViewAgent.tree = false
ViewAgent.graph = true
ViewAgent.2d = true
ViewAgent.3d = false

Period.Compute = 100
Period.Emergency = 500

Ice.Warn.Connections = "0"
Ice.Trace.Network = "0"
Ice.Trace.Protocol = "0"
Ice.MessageSizeMax = "20004800"


setRTPoseDSR = true
